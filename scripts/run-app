#!/bin/sh
set -eu
cd "$(dirname "$0")/.."
if ! command -v uv >/dev/null 2>&1; then
  echo "Error: HouseHunter requires uv. Install it from https://docs.astral.sh/uv/" >&2
  exit 1
fi
uv sync --no-dev
exec uv run --no-dev python -m househunter.run "$@"
